# ------------------------------------------------------------------
# CONFIGURACIÓN PARA GOOGLE DRIVE Y GOOGLE CLOUD STORAGE (GCS)
# ------------------------------------------------------------------

# ID de la carpeta raíz en Google Drive que la aplicación monitoreará.
# Ve a Google Drive, abre la carpeta y copia la última parte de la URL.
# Ejemplo: "https://drive.google.com/drive/folders/AQUI_VA_EL_ID"
ROOT_FOLDER_ID=""

# Nombres de las subcarpetas dentro de ROOT_FOLDER_ID que se deben procesar.
# Debe ser una lista de strings en formato JSON.
# Ejemplo: '["Facturas", "Recibos", "Documentos de Respaldo"]'
TARGET_FOLDER_NAMES='["doc de Respaldo"]'

# Nombre del bucket en Google Cloud Storage donde se guardará el estado (token) de la aplicación.
# Debe ser un nombre único a nivel global.
GCS_BUCKET_NAME="renamedriverfolderbucket"

# ------------------------------------------------------------------
# CONFIGURACIÓN DEL PROYECTO DE GOOGLE CLOUD (GCP)
# ------------------------------------------------------------------

# ID de tu proyecto de Google Cloud.
GCP_PROJECT_ID="cloud-functions"

# Región de GCP donde se ejecutará el modelo de IA (Gemini).
# Ejemplo: "us-central1", "southamerica-east1"
GCP_REGION="southamerica-east1"

# ------------------------------------------------------------------
# CONFIGURACIÓN DE PRUEBAS (TESTING)
# ------------------------------------------------------------------

# ID de una carpeta raíz en Google Drive dedicada para ejecutar las pruebas de integración.
# Puede ser la misma que ROOT_FOLDER_ID, pero se recomienda una separada para evitar modificar datos reales.
TEST_ROOT_FOLDER_ID=""

# ------------------------------------------------------------------
# CREDENCIALES Y CLAVES DE API (SECRETS)
# ------------------------------------------------------------------

# Clave de la cuenta de servicio (en formato JSON) codificada en Base64.
# 1. Descarga el archivo JSON de tu cuenta de servicio desde la consola de GCP.
# 2. Codifica el contenido completo del archivo a Base64.
#    - En PowerShell: [System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content -Raw "ruta\al\archivo.json")))
#    - En Linux/macOS: base64 -w 0 ruta/al/archivo.json
SERVICE_ACCOUNT_KEY_B64=""

# Clave de API para el modelo Gemini (Google AI).
# Obtenla desde Google AI Studio.
GEMINI_API_KEY=""