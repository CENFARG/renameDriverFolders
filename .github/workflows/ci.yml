name: CI Pipeline

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install linting tools
        run: pip install ruff

      - name: Lint core package
        run: ruff check packages/core-renombrador/src --ignore E501

      - name: Lint API server v3
        run: ruff check services/api-server-v3/src --ignore E501,E402

      - name: Lint worker v3
        run: ruff check services/worker-v3/src --ignore E501,E402

  test-core:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install core package with deps
        run: pip install -e packages/core-renombrador
        timeout-minutes: 10

      - name: Install test runner
        run: pip install pytest pytest-asyncio

      - name: Test core package
        run: |
          cd packages/core-renombrador
          python -m pytest tests/ -v --tb=short

  test-api-v3:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install core package with deps
        run: pip install -e packages/core-renombrador
        timeout-minutes: 10

      - name: Install test runner
        run: pip install pytest pytest-asyncio

      - name: Test API server v3
        run: |
          cd services/api-server-v3
          python -m pytest tests/ -v --tb=short

  test-worker-v3:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install core package with deps
        run: pip install -e packages/core-renombrador
        timeout-minutes: 10

      - name: Install test runner
        run: pip install pytest pytest-asyncio

      - name: Test worker v3
        run: |
          cd services/worker-v3
          python -m pytest tests/ -v --tb=short

  file-size-check:
    runs-on: ubuntu-latest
    needs: [test-core, test-api-v3, test-worker-v3]
    steps:
      - uses: actions/checkout@v4

      - name: Check file sizes (max 250 lines)
        run: |
          FAILED=0
          MAX_LINES=250

          check_files() {
            local dir="$1"
            while IFS= read -r file; do
              lines=$(wc -l < "$file")
              if [ "$lines" -gt "$MAX_LINES" ]; then
                echo "FAIL: $file has $lines lines (max: $MAX_LINES)"
                FAILED=1
              fi
            done < <(find "$dir" -name "*.py" -not -path "*/tests/*" -not -name "__init__.py" -not -name "conftest.py")
          }

          check_files "packages/core-renombrador/src"
          check_files "services/api-server-v3/src"
          check_files "services/worker-v3/src"

          if [ "$FAILED" -eq 1 ]; then
            echo ""
            echo "Some files exceed the 250-line limit."
            exit 1
          fi
          echo "All files within 250-line limit."
